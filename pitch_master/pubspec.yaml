name: pitch_master
description: "App per accordare strumenti a corde."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  sqflite: ^2.3.0
  path: ^1.8.3
  http: ^1.1.0
  permission_handler: ^11.0.0
  flutter_sound: ^9.2.13
  fft: ^0.5.1
  url_launcher: ^6.2.0
  file_picker: ^8.0.0
  path_provider: ^2.1.0
  open_filex: ^4.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true